#include "../utils/utilio.c"
#include <stdio.h>
int main(){
    char inp[100];
    printf("Enter a Math Problem:");
    scanf("%s",&inp);
    
    for (int i = 0; i < strlen(inp); i++)
    {
        if(inp[i]=="+"){
            
        }
    }
    
    
    return 0;
}